// Placeholder — the backend's Parents module is not implemented yet
// (Server/server.md § Excluded Modules). The Parents page currently runs
// entirely on local mock state (see src/store/dashboard-store.tsx). Add
// real calls here once Server/src/modules/parents exists.
export {};
